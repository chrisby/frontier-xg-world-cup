.env
__pycache__/
*.pyc
.venv/
.DS_Store
.claude/

# Secrets — never commit
kalshi_private_key.txt

# API cache — large, and the closest thing to the raw SportMonks feed (kept out)
data/cache.db
data/bets.db

# Raw historical results dump (leans toward raw-feed redistribution; large)
data/intl_results.csv

# Our derived analysis (ratings, lessons, goal shares) IS published — see
# the "Data & licensing" note in the README. Only raw feed data stays out.

# Personal notes
NOTES.md

# Copyrighted schedule PDF
FWC26 Match Schedule_v17_10042026_EN.pdf
